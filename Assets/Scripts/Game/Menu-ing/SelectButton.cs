using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SelectButton : MonoBehaviour
{
    public Transform actionListener;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void onClick()
    {
        //print(transform);
        //actionListener.GetComponent<LoginScripts>().menuSelection(transform.GetChild(0).GetComponent<Text>().text);
        
    }
}
