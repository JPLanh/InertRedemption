using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnergyGridRange : MonoBehaviour
{
    [SerializeField]
    Building dataEmitter;

    public Building getDataEmitter()
    {
        return dataEmitter;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
